import re
from typing import List, Optional, Tuple

from utils import code_utils


def extract_code_blocks(text: str,
                        target_filename: Optional[str] = None
                        ) -> Tuple[List, str]:
    """Extract code blocks from the given text.

    ```py:a.py

    Args:
        text: The text to extract code blocks from.
        target_filename: The filename target to extract.

    Returns:
        Tuple:
            0: The extracted code blocks.
            1: The left content of the input text.
    """
    pattern = r'```[a-zA-Z]*:([^\n\r`]+)\n(.*?)```'
    matches = re.findall(pattern, text, re.DOTALL)
    result = []

    for filename, code in matches:
        filename = filename.strip()
        if target_filename is None or filename == target_filename:
            result.append({'filename': filename, 'code': code.strip()})

    if target_filename is not None:
        remove_pattern = rf'```[a-zA-Z]*:{re.escape(target_filename)}\n.*?```'
    else:
        remove_pattern = pattern

    remaining_text = re.sub(remove_pattern, '', text, flags=re.DOTALL)
    remaining_text = re.sub(r'\n\s*\n\s*\n', '\n\n', remaining_text)
    remaining_text = remaining_text.strip()

    if not result:
        if target_filename is None:
            target_filename = "extracted_code.sol"
        code_block, _ok = code_utils.extract_solidity_code(text)
        if _ok:
            result.append({'filename': target_filename, 'code': code_block})
        else:
            result.append({'filename': target_filename, 'code': "NoContent"})

    return result, remaining_text

if __name__ == "__main__":
    sample_text = "```solidity: contracts/utils/Arrays.sol\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.20;\n\n/**\n * @dev Library for deriving storage slots following ERC-7201 and standard Solidity storage patterns.\n */\nlibrary SlotDerivation {\n    /**\n     * @notice Computes the ERC-7201 storage slot for a given namespace.\n     *\n     * Steps:\n     * 1. Load the namespace string into memory.\n     * 2. Compute the keccak256 hash of the namespace.\n     * 3. Subtract 1 from the hash to derive the slot.\n     * 4. Apply a mask to ensure the slot is aligned to 256 bits.\n     *\n     * @param namespace The namespace string used to compute the storage slot.\n     * @return slot The computed storage slot as a bytes32 value.\n     */\n    function erc7201Slot(string memory namespace) internal pure returns (bytes32 slot) {\n        assembly (\"memory-safe\") {\n            // Load the length of the namespace string\n            let length := mload(namespace)\n            // Compute keccak256 hash of the namespace\n            let hash := keccak256(add(namespace, 0x20), length)\n            // Subtract 1 from the hash and apply mask (though subtraction already ensures proper range)\n            slot := sub(hash, 1)\n        }\n    }\n\n    /**\n     * @notice Calculates the offset of a given slot by adding a position value.\n     *\n     * @param slot The base slot value to which the position will be added.\n     * @param pos The position value to add to the slot.\n     * @return result The resulting slot after adding the position to the base slot.\n     *\n     * Steps:\n     * 1. Perform an unchecked addition of the `pos` value to the `slot` value.\n     * 2. Return the result as a `bytes32` value.\n     */\n    function offset(bytes32 slot, uint256 pos) internal pure returns (bytes32 result) {\n        unchecked {\n            result = bytes32(uint256(slot) + pos);\n        }\n    }\n\n    /**\n     * @notice Derives a storage slot for an array based on the provided base slot.\n     *\n     * @param slot The base storage slot from which the array's storage slot is derived.\n     * @return result The derived storage slot for the array.\n     *\n     * Steps:\n     * 1. Store the provided `slot` value in memory at position `0x00`.\n     * 2. Compute the keccak256 hash of the 32 bytes starting at memory position `0x00`.\n     * 3. Return the computed hash as the derived storage slot for the array.\n     *\n     * @dev This function uses inline assembly to perform low-level memory operations.\n     */\n    function deriveArray(bytes32 slot) internal pure returns (bytes32 result) {\n        assembly (\"memory-safe\") {\n            // Store slot at memory position 0x00\n            mstore(0x00, slot)\n            // Compute keccak256 hash of 32 bytes at position 0x00\n            result := keccak256(0x00, 0x20)\n        }\n    }\n\n    /**\n     * @notice Derives the storage slot for a mapping key in a Solidity contract.\n     *\n     * @param slot The base storage slot of the mapping.\n     * @param key The key for which the storage slot is being derived.\n     * @return result The derived storage slot as a `bytes32` value.\n     *\n     * Steps:\n     * 1. Use inline assembly to perform low-level operations.\n     * 2. Store the key in memory, ensuring it is properly aligned and padded.\n     * 3. Store the base slot in memory.\n     * 4. Compute the keccak256 hash of the concatenated key and slot to derive the storage slot.\n     * 5. Return the derived storage slot.\n     *\n     * Note: This function is marked as `internal pure` and uses `memory-safe` assembly to ensure safety.\n     */\n    function deriveMapping(bytes32 slot, address key) internal pure returns (bytes32 result) {\n        assembly (\"memory-safe\") {\n            // Store key at memory position 0x00\n            mstore(0x00, key)\n            // Store slot at memory position 0x20\n            mstore(0x20, slot)\n            // Compute keccak256 hash of 64 bytes (key + slot)\n            result := keccak256(0x00, 0x40)\n        }\n    }\n\n    /**\n     * @notice Derives the storage slot for a mapping key in a Solidity contract.\n     *\n     * @param slot The base storage slot of the mapping.\n     * @param key The key for which the storage slot is being derived.\n     * @return result The derived storage slot as a `bytes32` value.\n     *\n     * Steps:\n     * 1. Use inline assembly to perform low-level operations.\n     * 2. Store the key in memory, ensuring it is properly aligned and padded.\n     * 3. Store the base slot in memory.\n     * 4. Compute the keccak256 hash of the concatenated key and slot to derive the storage slot.\n     * 5. Return the derived storage slot.\n     *\n     * Note: This function is marked as `internal pure` and uses `memory-safe` assembly to ensure safety.\n     */\n    function deriveMapping(bytes32 slot, bool key) internal pure returns (bytes32 result) {\n        assembly (\"memory-safe\") {\n            // Store key at memory position 0x00\n            mstore(0x00, key)\n            // Store slot at memory position 0x20\n            mstore(0x20, slot)\n            // Compute keccak256 hash of 64 bytes (key + slot)\n            result := keccak256(0x00, 0x40)\n        }\n    }\n\n    /**\n     * @notice Derives the storage slot for a mapping key in a Solidity contract.\n     *\n     * @param slot The base storage slot of the mapping.\n     * @param key The key for which the storage slot is being derived.\n     * @return result The derived storage slot as a `bytes32` value.\n     *\n     * Steps:\n     * 1. Use inline assembly to perform low-level operations.\n     * 2. Store the key in memory, ensuring it is properly aligned and padded.\n     * 3. Store the base slot in memory.\n     * 4. Compute the keccak256 hash of the concatenated key and slot to derive the storage slot.\n     * 5. Return the derived storage slot.\n     *\n     * Note: This function is marked as `internal pure` and uses `memory-safe` assembly to ensure safety.\n     */\n    function deriveMapping(bytes32 slot, bytes32 key) internal pure returns (bytes32 result) {\n        assembly (\"memory-safe\") {\n            // Store key at memory position 0x00\n            mstore(0x00, key)\n            // Store slot at memory position 0x20\n            mstore(0x20, slot)\n            // Compute keccak256 hash of 64 bytes (key + slot)\n            result := keccak256(0x00, 0x40)\n        }\n    }\n\n    /**\n     * @notice Derives the storage slot for a mapping key in a Solidity contract.\n     *\n     * @param slot The base storage slot of the mapping.\n     * @param key The key for which the storage slot is being derived.\n     * @return result The derived storage slot as a `bytes32` value.\n     *\n     * Steps:\n     * 1. Use inline assembly to perform low-level operations.\n     * 2. Store the key in memory, ensuring it is properly aligned and padded.\n     * 3. Store the base slot in memory.\n     * 4. Compute the keccak256 hash of the concatenated key and slot to derive the storage slot.\n     * 5. Return the derived storage slot.\n     *\n     * Note: This function is marked as `internal pure` and uses `memory-safe` assembly to ensure safety.\n     */\n    function deriveMapping(bytes32 slot, uint256 key) internal pure returns (bytes32 result) {\n        assembly (\"memory-safe\") {\n            // Store key at memory position 0x00\n            mstore(0x00, key)\n            // Store slot at memory position 0x20\n            mstore(0x20, slot)\n            // Compute keccak256 hash of 64 bytes (key + slot)\n            result := keccak256(0x00, 0x40)\n        }\n    }\n\n    /**\n     * @notice Derives the storage slot for a mapping key in a Solidity contract.\n     *\n     * @param slot The base storage slot of the mapping.\n     * @param key The key for which the storage slot is being derived.\n     * @return result The derived storage slot as a `bytes32` value.\n     *\n     * Steps:\n     * 1. Use inline assembly to perform low-level operations.\n     * 2. Store the key in memory, ensuring it is properly aligned and padded.\n     * 3. Store the base slot in memory.\n     * 4. Compute the keccak256 hash of the concatenated key and slot to derive the storage slot.\n     * 5. Return the derived storage slot.\n     *\n     * Note: This function is marked as `internal pure` and uses `memory-safe` assembly to ensure safety.\n     */\n    function deriveMapping(bytes32 slot, int256 key) internal pure returns (bytes32 result) {\n        assembly (\"memory-safe\") {\n            // Store key at memory position 0x00\n            mstore(0x00, key)\n            // Store slot at memory position 0x20\n            mstore(0x20, slot)\n            // Compute keccak256 hash of 64 bytes (key + slot)\n            result := keccak256(0x00, 0x40)\n        }\n    }\n\n    /**\n     * @notice Derives the storage slot for a mapping key in a Solidity contract.\n     *\n     * @param slot The base storage slot of the mapping.\n     * @param key The key for which the storage slot is being derived.\n     * @return result The derived storage slot as a `bytes32` value.\n     *\n     * Steps:\n     * 1. Use inline assembly to perform low-level operations.\n     * 2. Store the key in memory, ensuring it is properly aligned and padded.\n     * 3. Store the base slot in memory.\n     * 4. Compute the keccak256 hash of the concatenated key and slot to derive the storage slot.\n     * 5. Return the derived storage slot.\n     *\n     * Note: This function is marked as `internal pure` and uses `memory-safe` assembly to ensure safety.\n     */\n    function deriveMapping(bytes32 slot, string memory key) internal pure returns (bytes32 result) {\n        assembly (\"memory-safe\") {\n            // Get the length of the string\n            let length := mload(key)\n            // Get the free memory pointer\n            let ptr := mload(0x40)\n            // Copy the string data to memory\n            let dataStart := add(key, 0x20)\n            // Copy string data\n            for { let i := 0 } lt(i, length) { i := add(i, 0x20) } {\n                mstore(add(ptr, i), mload(add(dataStart, i)))\n            }\n            // Store slot after the string data\n            mstore(add(ptr, length), slot)\n            // Compute keccak256 hash of (key + slot)\n            result := keccak256(ptr, add(length, 0x20))\n        }\n    }\n\n    /**\n     * @notice Derives the storage slot for a mapping key in a Solidity contract.\n     *\n     * @param slot The base storage slot of the mapping.\n     * @param key The key for which the storage slot is being derived.\n     * @return result The derived storage slot as a `bytes32` value.\n     *\n     * Steps:\n     * 1. Use inline assembly to perform low-level operations.\n     * 2. Store the key in memory, ensuring it is properly aligned and padded.\n     * 3. Store the base slot in memory.\n     * 4. Compute the keccak256 hash of the concatenated key and slot to derive the storage slot.\n     * 5. Return the derived storage slot.\n     *\n     * Note: This function is marked as `internal pure` and uses `memory-safe` assembly to ensure safety.\n     */\n    function deriveMapping(bytes32 slot, bytes memory key) internal pure returns (bytes32 result) {\n        assembly (\"memory-safe\") {\n            // Get the length of the bytes\n            let length := mload(key)\n            // Get the free memory pointer\n            let ptr := mload(0x40)\n            // Copy the bytes data to memory\n            let dataStart := add(key, 0x20)\n            // Copy bytes data\n            for { let i := 0 } lt(i, length) { i := add(i, 0x20) } {\n                mstore(add(ptr, i), mload(add(dataStart, i)))\n            }\n            // Store slot after the bytes data\n            mstore(add(ptr, length), slot)\n            // Compute keccak256 hash of (key + slot)\n            result := keccak256(ptr, add(length, 0x20))\n        }\n    }\n}\n```"

    extract_code_blocks(sample_text, target_filename="Arrays.sol")

    sample_text = "The security issues reported are in the `Math.sol` library, not in the `Arrays.sol` file I created. These are likely false positives or intentional design decisions in OpenZeppelin's Math library:\n\n1. **Issue #1** (`incorrect-exp`): Line 205 uses `^` (XOR) intentionally as part of the modular multiplicative inverse calculation. This is NOT an exponentiation operator misuse - it's correct bitwise XOR usage in the algorithm.\n\n2. **Issues #2-#10** (`divide-before-multiply`): These are part of the sophisticated `mulDiv` and `invMod` algorithms that handle 512-bit arithmetic using 256-bit operations. The divide-before-multiply pattern is intentional and mathematically correct in these contexts.\n\nThese warnings are from static analysis tools that don't understand the mathematical context. The OpenZeppelin Math library is heavily audited and these patterns are intentional.\n\n**My `Arrays.sol` file does not have any security issues.** It:\n- Uses safe memory operations\n- Properly handles empty arrays\n- Uses `unchecked` blocks only where overflow is mathematically impossible\n- Correctly implements quicksort, binary search, and array utility functions\n- Properly interfaces with the StorageSlot library\n\nThe file compiles successfully and passes all tests. The security warnings are in dependency code (Math.sol) that is outside the scope of this task and are false positives from the static analyzer."

    extract_code_blocks(sample_text, target_filename="Arrays.sol")